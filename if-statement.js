// Mauricio Rojas
// Saturday June 2nd, 2018


// Declares a variable named fruit and assigns it to the value of "Orange"
var fruit = 'orange';

//If statement that triggers when the length of the fruit string is greater than 5
if(fruit.length > 5 ){
  // If the length of the fruit string is greater than 5 than it Logs that to the console
  return console.log('The fruit name has more than five characters.');
// If not, then it Logs that it isn't
} else {
  return console.log('The fruit name has five characters or less.');
}
